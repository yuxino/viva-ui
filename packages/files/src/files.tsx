import * as React from "react";

// const Files: React.FC = ({ menus, onChange }) => {
//   return (
//     <ul>
//       <li onClick={() => onChange(menus.route)}>{menus.name}</li>
//       {menus.children.map(item => (
//         <Files onChange={onChange} key={item.id} menus={item} />
//       ))}
//     </ul>
//   );
// };

const Files = () => <div>hello world</div>;

export default Files;
